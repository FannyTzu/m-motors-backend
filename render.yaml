services:
  - type: web
    name: m-motors-backend
    env: node
    buildCommand: NODE_ENV=development npm install && npx prisma migrate deploy && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
